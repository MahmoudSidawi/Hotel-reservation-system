import AdminDashboard from "@/components/admin/AdminDashboard";

export const metadata = {
  title: "Admin Dashboard | Velora Hotel Management",
  description: "Velora Hotel Management System — Full operational control center for administrators",
};

export default function AdminPage() {
  return <AdminDashboard />;
}
